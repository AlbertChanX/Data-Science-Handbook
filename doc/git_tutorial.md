
```
echo "# Data-Science-Handbook" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/githubcyc/Data-Science-Handbook.git
git push -u origin master
```

```
// lastest commit
git reset --hard HEAD
// back to last commit
git reset --hard HEAD^

// git branch <name> && git checkout <name>
git checkout -b <name>
```